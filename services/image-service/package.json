{
  "name": "image-service",
  "version": "1.0.0",
  "description": "AWS Lambda — S3-triggered thumbnail generation with sharp",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r function.zip dist/ node_modules/"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "sharp": "^0.33.4"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^20.0.0",
    "typescript": "^5.1.3"
  }
}
